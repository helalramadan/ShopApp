import 'package:flutter/material.dart';

class ShopCatgories extends StatelessWidget {
  const ShopCatgories({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Catgories'),
    );
  }
}
